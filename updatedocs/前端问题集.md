# 前端问题集

## 问题1：点击"查看分块"时页面退出

### 问题描述
当用户在文档预览页面点击"查看分块"按钮时，页面会意外退出，无法正常显示分块内容。

### 错误信息
```
index-BphDrp0I.js:3 TypeError: W.replace is not a function 
 Please report this to `https://github.com/markedjs/marked.` 
     at r (KnowledgeBase-CVKXrj0k.js:2:2391) 
     at b (KnowledgeBase-CVKXrj0k.js:4:1070) 
     at Object.M [as highlightAuto] (KnowledgeBase-CVKXrj0k.js:4:1237) 
     at s.code (KnowledgeBase-CVKXrj0k.js:16:343) 
     at a.<computed> (index-BphDrp0I.js:63:5694) 
     at Sy.parse (index-BphDrp0I.js:63:1924) 
     at parse (index-BphDrp0I.js:63:1327) 
     at xI.parse (index-BphDrp0I.js:63:8268) 
     at Jt [as parse] (index-BphDrp0I.js:64:239) 
     at h (KnowledgeBase-CVKXrj0k.js:25:51)
```

### 问题原因
在 `doc-content.vue` 文件的 `renderer.code` 函数中，传递给 `highlight.js` 的 `text` 参数可能不是字符串类型，导致 `highlightAuto` 函数调用 `replace` 方法时出现类型错误。

### 解决方案
修改 `renderer.code` 函数，确保传递给 `highlight.js` 的内容始终是字符串类型：

1. 修改参数类型：将 `text: string` 改为 `text: any`
2. 添加类型转换：添加 `const safeText = typeof text === 'string' ? text : String(text || '')`
3. 更新函数调用：将所有 `text` 参数替换为 `safeText`

### 修复代码
```javascript
// 自定义代码块渲染器，只显示语言标签
renderer.code = function (text: any, lang?: string) {
  // 确保text是字符串
  const safeText = typeof text === 'string' ? text : String(text || '');
  // Mermaid 图表处理
  if (lang === 'mermaid') {
    // 生成唯一ID
    const id = `mermaid-${++mermaidRenderCount}`;
    // 返回带有 mermaid 类的 div，后续由 mermaid.run() 处理
    return `<div class="mermaid" id="${id}">${safeText}</div>`;
  }

  let detectedLang = lang;
  let highlighted = '';
  if (lang && hljs.getLanguage(lang)) {
    try {
      highlighted = hljs.highlight(safeText, { language: lang }).value;
    } catch (e) {
      highlighted = hljs.highlightAuto(safeText).value;
      detectedLang = hljs.highlightAuto(safeText).language || lang;
    }
  } else {
    const auto = hljs.highlightAuto(safeText);
    highlighted = auto.value;
    detectedLang = auto.language || lang;
  }
  const displayLang = detectedLang || 'Code';
  return `
    <div class="code-block-wrapper">
      <div class="code-block-header">
        <span class="code-block-lang">${displayLang}</span>
      </div>
      <pre class="code-block-pre"><code class="hljs language-${detectedLang || ''}">${highlighted}</code></pre>
    </div>
  `;
};
```

### 验证步骤
1. 重新构建前端代码：`npm run build`
2. 打开文档预览页面
3. 点击"查看分块"按钮
4. 验证页面是否能正常显示分块内容，不再退出

### 修复效果
修复后，用户可以正常点击"查看分块"按钮，页面不会意外退出，分块内容能够正常显示。
